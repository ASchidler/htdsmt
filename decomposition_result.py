

class DecompositionResult:
    def __init__(self, size, decomposition, arcs, ordering, weights):
        self.size = size
        self.decomposition = decomposition
        self.arcs = arcs
        self.ordering = ordering
        self.weights = weights
